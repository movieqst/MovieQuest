form {
  width: 100%;
  max-width: 500px;
  display: flex;
  gap: 8px;
  margin-bottom: 15px; /* moved from chatlog, since form is now on top */
}

#chatlog {
  width: 100%;
  max-width: 500px;
  height: 140px; /* was 280px — now half */
  overflow-y: auto;
  background: #111;
  border: 1px solid #333;
  padding: 15px;
  font-size: 14px;
  line-height: 1.6;
}
